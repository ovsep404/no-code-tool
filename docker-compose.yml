version: '3.8'

services:
  web:
    build: .
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
    environment:
      - APACHE_RUN_USER=www-data
      - APACHE_RUN_GROUP=www-data
    depends_on:
      - db
    restart: always

  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: resumodb
      MYSQL_USER: resumouser
      MYSQL_PASSWORD: resumopassword
    volumes:
      - db_data:/var/lib/mysql
    restart: always

volumes:
  db_data: